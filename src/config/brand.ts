/** Point this at any image in `/public` when you add assets (e.g. `/hero.jpg`). */
export const HERO_IMAGE_SRC = "/glowly-hero.svg";

export const HERO_IMAGE_ALT =
  "Glowly store hero — replace glowly-hero.svg or set HERO_IMAGE_SRC in src/config/brand.ts";
